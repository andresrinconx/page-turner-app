export enum PhotosPermissionStatus {
  CHECKING = "checking",
  GRANTED = "granted",
  DENIED = "denied",
  BLOCKED = "blocked",
  LIMITED = "limited",
  UNAVAILABLE = "unavailable",
  UNDETERMINED = "undetermined",
}
